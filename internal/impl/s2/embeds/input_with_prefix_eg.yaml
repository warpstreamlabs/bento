cache_resources:
  - label: s2_seq_num
    file:
      directory: s2_seq_num_cache

input:
  label: s2_input
  s2:
    basin: my-favorite-basin
    streams: my-favorite-prefix/
    auth_token: "${S2_AUTH_TOKEN}"
    cache: s2_seq_num

output:
  label: stdout
  stdout:
    codec: lines
