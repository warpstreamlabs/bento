---
title: datadog_logs
slug: datadog_logs
type: input
status: beta
categories: ["Services"]
---

<!--
     THIS FILE IS AUTOGENERATED!

     To make changes please edit the corresponding source file under internal/impl/<provider>.
-->

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

:::caution BETA
This component is mostly stable but breaking changes could still be made outside of major version releases if a fundamental problem with the component is found.
:::
Reads log entries from the Datadog Logs API.


<Tabs defaultValue="common" values={[
  { label: 'Common', value: 'common', },
  { label: 'Advanced', value: 'advanced', },
]}>

<TabItem value="common">

```yml
# Common config fields, showing default values
input:
  label: ""
  datadog_logs:
    api_key: "" # No default (optional)
    app_key: "" # No default (optional)
    query: '*'
    from: "" # No default (optional)
    to: "" # No default (optional)
```

</TabItem>
<TabItem value="advanced">

```yml
# All config fields, showing default values
input:
  label: ""
  datadog_logs:
    api_key: "" # No default (optional)
    app_key: "" # No default (optional)
    site: datadoghq.com # No default (optional)
    query: '*'
    from: "" # No default (optional)
    to: "" # No default (optional)
    start_from_oldest: true
    indexes: [] # No default (optional)
    page_limit: 100
    http_client:
      retry_enabled: true
      max_retries: 3
      timeout: 60s
```

</TabItem>
</Tabs>

Queries the Datadog Logs search API and emits one batch of messages per page of results.

When a page is exhausted and no `to` is configured, the input slides the time window forward to the last seen log timestamp and waits `poll_interval` before querying again, providing a live tail behaviour.

### Metadata

```text
- datadog_log_host
- datadog_log_service
- datadog_log_tags
- datadog_log_status
- datadog_log_timestamp
- datadog_log_<attribute_key>
```

### Authentication

Set `api_key` and `app_key` explicitly or via the `DD_API_KEY` and `DD_APP_KEY` environment variables. Both keys are required to query logs.

## Fields

### `api_key`

Datadog API key. Falls back to the `DD_API_KEY` environment variable if unset.
:::warning Secret
This field contains sensitive information that usually shouldn't be added to a config directly, read our [secrets page for more info](/docs/configuration/secrets).
:::


Type: `string`  

### `app_key`

Datadog application key. Falls back to the `DD_APP_KEY` environment variable if unset. Required for querying logs.
:::warning Secret
This field contains sensitive information that usually shouldn't be added to a config directly, read our [secrets page for more info](/docs/configuration/secrets).
:::


Type: `string`  

### `site`

Datadog site to query. Falls back to `DD_SITE`, then `datadoghq.com`.


Type: `string`  

```yml
# Examples

site: datadoghq.com

site: datadoghq.eu

site: us3.datadoghq.com

site: us5.datadoghq.com
```

### `query`

Log search query.


Type: `string`  
Default: `"*"`  

### `from`

Start of the query time window as an RFC 3339 timestamp. Defaults to 15 minutes before the input connects.


Type: `string`  

### `to`

End of the query time window as an RFC 3339 timestamp. If unset the window slides forward continuously.


Type: `string`  

### `start_from_oldest`

When true, logs are returned oldest-first (ascending). When false, newest-first (descending).


Type: `bool`  
Default: `true`  

### `indexes`

Log indexes to search. Defaults to all indexes.


Type: `array`  

### `page_limit`

Maximum number of log entries per page request.


Type: `int`  
Default: `100`  

### `http_client`

Sorry! This field is missing documentation.


Type: `object`  

### `http_client.retry_enabled`

Enable automatic retries on API errors.


Type: `bool`  
Default: `true`  

### `http_client.max_retries`

Maximum number of retries per request.


Type: `int`  
Default: `3`  

### `http_client.timeout`

Maximum total time to spend retrying a single request.


Type: `string`  
Default: `"60s"`  


